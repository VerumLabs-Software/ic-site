//- Need sync with createImage function
mixin image-jpg(src, alt)
  - const image = `assets/images/${src}`

  +b.image&attributes(attributes)
    picture
      source(srcset=`${image}.webp, ${image}@2x.webp 2x, ${image}@3x.webp 3x`, type='image/webp')
      img(src=`${image}.jpg, ${image}@2x.jpg 2x, ${image}@3x.jpg 3x`, alt=alt || '')
    block

//- Need sync with createImage function
mixin image-png(src, alt)
  - const image = `assets/images/${src}`

  +b.image&attributes(attributes)
    picture
      source(srcset=`${image}.webp, ${image}@2x.webp 2x, ${image}@3x.webp 3x`, type='image/webp')
      img(src=`${image}.png, ${image}@2x.png 2x, ${image}@3x.png 3x`, alt=alt || '')
    block

mixin image-svg(src, alt)
  +b.image&attributes(attributes)
    img(src=`assets/images/${src}.svg`, alt=alt || '')
    block

mixin parallax-image(src)
  +b.parallax-image(data-parallax="scroll", data-image-src=`assets/images/${src}.png`)&attributes(attributes)
