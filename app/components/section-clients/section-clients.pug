mixin section-clients()
  +b.SECTION(data-section="").section-clients#clients&attributes(attributes)
    .container
      +title Meet our clients
      +text._sm.__description As one of the top video production companies in Miami, NYC and the Hamptons, we work with some of the world’s most influential luxury brands to create world-class video production for marketing and sales. Meet some of our clients below.
      //- Desktop
      +slider(false, true).__slider.js-slider.hide-sm
        +slide
          +grid
            each client in getData('client-testimonials')
              +col(4)
                +client-block(client)
      //- Mobile
      +slider(false, false).__slider.js-slider-free.show-sm
        each client in getData('client-testimonials')
          +slide
            +client-block(client)
